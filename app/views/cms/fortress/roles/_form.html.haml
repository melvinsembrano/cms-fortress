= form.text_field :name
= form.text_area :description, :rows => 2

= form.form_group :class => 'form-actions' do
  = form.submit @cms_fortress_role.new_record? ? 'Create' : 'Update', :class => 'btn btn-primary'

