= form_for @cms_fortress_role do |f|
  - if @cms_fortress_role.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cms_fortress_role.errors.count, "error")} prohibited this cms_fortress_role from being saved:"
      %ul
        - @cms_fortress_role.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description
  .actions
    = f.submit 'Save'
